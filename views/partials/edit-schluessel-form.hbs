<form name="notiz" id="notiz-form" method="POST" action="/editschluessel/{{rows.Id}}">
    <div class="card">        
        <div class="card-body">
            <label for="schluessel">Notiz:</label>
            <input name="notiz" id="notiz" value="{{rows.Notiz}}" class="notiz" type="text"/> 
        </div>            
        <div class="card-footer">
            <input type="hidden" name="id" value="{{rows.Id}}" />
            <input type="submit" value="Notiz Speichern" id="auswählen-btn" name="action" class="notiz-btn" />
        </div>
    </div>
</form>